name: Update EPG File NOR2.xml

on:
  schedule:
    - cron: '45 5,13 * * *'  # Ejecuta a las 5:45 AM y 1:45 PM cada día.
  workflow_dispatch: # Permite ejecutar manualmente el flujo de trabajo.

jobs:
  update_epg:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Remove existing NOR2.xml
      run: |
        rm -f ./NOR2.xml
